               // 3rd divison
function scrollDestinations(distance) {
    const container = document.querySelector('.destination-row');
    container.scrollBy({ left: distance, behavior: 'smooth' });

   

}
